version: "3"

services:
  nginx:
    image: nginx:stable-alpine
    restart: always
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./app:/app
      - ./rss/config.php:/app/config.php
    links:
      - ttrss
    depends_on:
      - ttrss
    ports:
     - "8080:80"

  ttrss:
    build:
        context: rss/
        dockerfile: Dockerfile
    restart: always
    volumes:
      - ./app:/app
      - ./rss/config.php:/app/config.php
    links:
      - postgres
    depends_on:
      - postgres

  postgres:
    build:
        context: postgres/
        dockerfile: Dockerfile
    restart: always
    environment:
      POSTGRES_USER: ttrss
      POSTGRES_PASSWORD: ttrss
      POSTGRES_DB: ttrss
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./app/schema/ttrss_schema_pgsql.sql:/tmp/ttrss_schema_pgsql.sql

volumes:
  pgdata:
  # ttrss:
