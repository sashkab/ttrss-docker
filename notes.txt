docker-compose build --no-cache caddy && docker-compose stop caddy && docker-compose rm -f caddy && docker-compose up -d caddy

docker-compose build --no-cache phpfpm && docker-compose stop phpfpm && docker-compose rm -f phpfpm && docker-compose up -d phpfpm

docker-compose build --no-cache update_daemon && docker-compose stop update_daemon && docker-compose rm -f update_daemon && docker-compose up -d update_daemon
